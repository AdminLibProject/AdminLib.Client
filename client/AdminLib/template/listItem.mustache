{{!

Data structure :

   [root] :
      - <string>  classes
      - <string>  title    : Title of the table. Will be use for the portlet
      - <Field[]> fields   : List of fields. Will be use for column headers
      - <Item[]>  items    : List of items to display
      - <number>  id       : ID of the listItem

   Field :
      Represent a field of item.
      - <string> class : Class of the field. Will be added to the column header
      - <string> label : Label of the header. If undefined, the the code will be used.
      - <string> code : Code of the field. If the field is undefined, then the code is use as a label. In this case, the "firstLetterUppercase" class will be used.

   Item :
      Correspond to a row of the table.

      - <number>      index  : ID of the item.
      - <ItemField[]> fields : Fields of the item. Each field contain a data that will be display in a single cell.

   ItemField :
      Represent a data of an item.

      - <string> code        : Code of the field
      - <number> fieldNumber : Number of the field
      - <string> class       : Class of the cell
      - <string> link        : If defined, then the data will be a link to the given value. (ex: link = "www.google.fr" ==> <a href="www.google.fr">some value</a>
      - <string> value       : Value to display
}}

<div class="{{class}}" data-id="{{id}}">
   <div class="portlet box blue {{classes}}" data-AdminModule="portlet">
      <div class="portlet-title">
         <div class="caption">
            {{title}}
         </div>
      </div>
      <div class="portlet-body">
         <div class="row">
            <div class="col-md-12">
               <table class="table datatable table-bordered table-striped table-hover flip-content no-footer">

                  <thead class="flip-content">
                     <tr>
                        {{#fields}}
                           <th class="{{class}} {{^label}}firstLetterUppercase{{/label}}">
                              {{#label}}{{label}}{{/label}}
                              {{^label}}{{code}}{{/label}}
                           </th>
                        {{/fields}}
                     </tr>
                  </thead>

                  <tbody>
                     {{#items}}
                        <tr data-index="{{index}}">
                           {{#fields}}
                              <td class="{{type}}" data-code="{{code}}" data-field#="{{fieldNumber}}">
                                 {{#link}}
                                    <a href="{{link}}">{{value}}</a>
                                 {{/link}}
                                 {{^link}}
                                    {{value}}
                                 {{/link}}
                              </td>
                           {{/fields}}
                        </tr>
                     {{/items}}
                  </tbody>

               </table>
            </div>
         </div>
      </div>
   </div>
</div>