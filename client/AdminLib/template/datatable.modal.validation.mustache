{{!

Use : Modal displayed when an action has failed (error or warning) or when informations is provided

Data model :
   [Root] :
      - {string} title           Title of the modal
      - {string} message         Message to display. HTML tags will be interpreted
      - {Button} continueButton  Informations about the "continue action" button
      - {Button} cancelButotn    Informations about the "cancel action" button

   Button :
      - {string} class
      - {string} icon
      - {string} label
}}

<div class="modal fade modal-overflow" tabindex="-1" >

   <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
      <h4 class="modal-title">{{title}}</h4>
   </div>

   <div class="modal-body">
      {{{message}}}
   </div>

   <div class="modal-footer">

      {{#continueButton}}
         <button type="button" class="btn {{class}}" id="continue">
            {{#icon}}<i class="{{icon}}"></i> {{/icon}}
            {{label}}
         </button>
     {{/continueButton}}

     {{#cancelButton}}
        <button type="button" class="btn {{class}}" data-dismiss="modal" id="cancel">
           {{#icon}}<i class="{{icon}}"></i> {{/icon}}
           {{label}}
        </button>
     {{/cancelButton}}

  </div>

</div>;